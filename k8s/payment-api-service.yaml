apiVersion: v1
kind: Service
metadata:
  name: g73-payment-api
  # annotations:
  #   service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  selector:
    app: g73-payment-api
  ports:
    - protocol: TCP
      port: 8081
      targetPort: 30001
      nodePort: 30001
  type: ClusterIP