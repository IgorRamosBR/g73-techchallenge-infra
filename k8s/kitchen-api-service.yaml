apiVersion: v1
kind: Service
metadata:
  name: g73-kitchen-api-service
  annotations:
    service.beta.kubernetes.io/aws-load-balancer-type: nlb
spec:
  selector:
    app: g37-kitchen-api
  ports:
    - protocol: TCP
      port: 8082
      targetPort: 8082
      nodePort: 30002
  type: LoadBalancer